pluginManagement {
    plugins {
        id 'io.micrc.core.gradle.plugin' version 'v0.0.64'
    }
    repositories {
        mavenLocal()
        maven {
            url "https://repo.it.ouxxa.com/repository/maven-hub/"
        }
    }
}

rootProject.name = 'democontext-service'
